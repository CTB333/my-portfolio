{
  "scripts": {
    "build": "(npm install || true) && npx tsc",
    "postbuild": "run-script-os",
    "postbuild:windows": "(copy package*.json dist || true) && (cd dist || true) && npm i --omit=dev",
    "postbuild:default": "cp package*.json dist && cd dist && npm i --omit=dev"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.19.17",
    "@types/validatorjs": "^3.15.5",
    "run-script-os": "^1.1.6",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@typegoose/typegoose": "^12.0",
    "@types/node-fetch": "^2.6.13",
    "mongoose": "^8.0",
    "node-fetch": "^3.3.2",
    "validatorjs": "^3.22.1"
  }
}
